using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FirstSetPosition : MenuButton
{
    // Start is called before the first frame update
    void Start()
    {
        SnapTo();
    }
}
